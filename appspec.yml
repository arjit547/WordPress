version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
    overwrite: true
    permissions:
      - object: wp-admin/
        pattern: "**"
hooks:
  BeforeInstall:
    - location: 
      runas: root
